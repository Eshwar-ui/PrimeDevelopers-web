# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.seed

# Supabase local dev
supabase/.temp
supabase/.branches

# Generated 3D test fixture — rebuild with scripts/make-test-model.mjs.
# Kept out of the repo so it never ships to the live site.
test-model.glb

# Production build inputs (values live in the deploy environment)
.env.production

# Editor/tool backups of env files — these carry real secrets.
.env.bak
.env.*.bak
*.env.bak
