# Prime Developers API (apps/api). In production this is the Render service URL.
# The browser holds no Supabase credentials — all data goes through the API,
# and image/model URLs are public bucket links needing no key.
VITE_API_BASE_URL=http://localhost:3001
